export const GameStatus = Object.freeze({
    stopped: 'stopped',
    started: 'started'
});

export const CellOwner = Object.freeze({
    none: 'none',
    vacant: 'vacant',
    user: 'user',
    comp: 'comp'
});